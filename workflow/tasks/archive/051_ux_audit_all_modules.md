# Task 051: UX Audit — All Modules

## Context
- **Design Feature:** N/A (cross-cutting content quality task)
- **Milestone:** Pedagogical Quality Pass — All Modules
- **Relevant Files:**
  - `.claude/rules/MEMORY.md` — (read-only: /test-ux agent definition)
  - `00_Setup/README.md` — (read-only: module under review)
  - `01_Host_API/README.md` — (read-only: module under review)
  - `02_Projects/A_Multimedia/README.md` — (read-only: module under review)
  - `02_Projects/B_Graphics_HPC/README.md` — (read-only: module under review)
  - `02_Projects/C_Robotics_ROS2/README.md` — (read-only: module under review)
  - `99_Toolbox/README.md` — (read-only: module under review)
  - `04_Addons/README.md` — (read-only: module under review)
  - `workflow/tasks/ux_audit_report.md` — (new file: aggregated findings)

## Objective
Run `/test-ux` on all seven module trees, aggregate the Pedagogical Gaps & UX Friction reports into a single findings document, present them to the human for triage, then implement the selected fixes.

## Constraints & Rules
- `/test-ux` scope per module: read the module `README.md` and any source files referenced inline (e.g., `src/*.cpp`, `kernels/*.cl`). Do NOT read unrelated modules during a single module pass.
- Only README and documentation files may be edited during the fix phase. Do NOT modify `.cpp`, `.cl`, or `CMakeLists.txt`.
- Show a diff before applying every fix. Apply fixes only after the human selection step (Item C).
- Record all file edits in the findings report.

---

## Implementation

### A — Per-Module /test-ux Pass

**Problem:** No systematic UX walkthrough has been run. Junior students may encounter missing prerequisites, unexplained commands, or steep cognitive leaps when following module READMEs.

**Decision:** Run `/test-ux` on each module in sequence, accumulating findings before any edits are made.

**Action:**

For each module below, invoke the `/test-ux` procedure (simulate a junior student walkthrough: read the README top-to-bottom, mentally execute each bash command, identify missing prerequisites or unexplained jumps):

1. `00_Setup/` — entry point; prerequisite for all other modules.
2. `01_Host_API/` — first coding module.
3. `02_Projects/A_Multimedia/` — Path A.
4. `02_Projects/B_Graphics_HPC/` — Path B.
5. `02_Projects/C_Robotics_ROS2/` — Path C.
6. `99_Toolbox/` — optimization reference.
7. `04_Addons/` — extension modules.

---

### B — Aggregate Findings Report

**Problem:** Per-module findings are ephemeral unless persisted before the human review step.

**Decision:** Write all findings to `workflow/tasks/ux_audit_report.md` before any fixes are applied.

**Action:**

Create `workflow/tasks/ux_audit_report.md` with the structure below. Every issue must include a severity tag and a proposed fix so the human can make a fast triage decision.

```markdown
# UX Audit Report — All Modules
<!-- Generated by Task 051. Human: tick boxes to select issues for fixing, then run /implement. -->

## [Module Name]
### Pedagogical Gaps
| # | Issue | Location | Severity | Proposed Fix | Fix? |
|---|-------|----------|----------|--------------|------|
| 1 | ... | README.md:L42 | HIGH/MED/LOW | ... | [ ] |

### UX Friction Points
| # | Issue | Location | Severity | Proposed Fix | Fix? |
|---|-------|----------|----------|--------------|------|
| 1 | ... | README.md:L10 | HIGH/MED/LOW | ... | [ ] |
```

---

### C — MANUAL: Human Triage

**Problem:** Not all identified issues are worth fixing — some may be intentional design choices.

**Decision:** The human reviews `workflow/tasks/ux_audit_report.md`, ticks the `Fix?` checkbox for each issue to address, and then triggers the fix phase by invoking `/implement` on this task.

**Action:**

1. Open `workflow/tasks/ux_audit_report.md`.
2. For each row, tick `[x]` in the `Fix?` column for issues to resolve; leave `[ ]` for those to skip.
3. Optionally annotate rows with `<!-- note: ... -->` to guide the fix.
4. Invoke `/implement` to proceed to Item D.

---

### D — Apply Selected Fixes

**Problem:** Ticked issues must be resolved before the report can be closed.

**Decision:** Apply fixes only to items with `[x]` in the `Fix?` column. Edit only README and documentation files; never source code.

**Action:**

1. Read `workflow/tasks/ux_audit_report.md`; collect all rows where `Fix?` = `[x]`.
2. For each selected issue, show the diff and apply using `str_replace`.
3. After all edits are applied, update the `Fix?` column value to `DONE` for completed items.
4. Re-read each edited README to confirm the fix resolves the stated issue without introducing new friction.

---

## Definition of Done (DoD)

- [x] `/test-ux` has been run on all 7 modules (`00_Setup`, `01_Host_API`, `A_Multimedia`, `B_Graphics_HPC`, `C_Robotics_ROS2`, `99_Toolbox`, `04_Addons`).
- [x] `workflow/tasks/ux_audit_report.md` exists and contains a findings table for every module (at least "no issues found" if clean).
- [x] MANUAL: Human has reviewed `workflow/tasks/ux_audit_report.md` and ticked `[x]` for all issues to fix.
- [x] All `[x]`-selected issues have been addressed; their `Fix?` cell updated to `DONE`.
- [x] No `.cpp`, `.cl`, or `CMakeLists.txt` files were modified.
- [x] `workflow/tasks/ux_audit_report.md` contains a `### Changed Files` section listing every README that was edited.

---

## Execution Report
<!-- Filled by @tester after Items A–B; @coder after Item D. -->

- **Status:** COMPLETE
- **Session:** 2026-03-22

### Completed
| Item | Action |
|------|--------|
| A — Per-Module /test-ux Pass | Run in parallel across all 7 modules. 19 HIGH, ~50 MED, ~30 LOW issues found. |
| B — Aggregate Findings Report | Created `workflow/tasks/ux_audit_report.md` with findings tables for all 7 modules. |
| C — Human Triage | Human ticked [x] for selected issues in `ux_audit_report.md`. |
| D — Apply Selected Fixes | All [x] items applied across 30 files. 3 items skipped (source files: main.cpp×2, CMakeLists.txt×1). |

### Validation

```text
All 7 /test-ux agents completed successfully.
ux_audit_report.md created at workflow/tasks/ux_audit_report.md.
All [x] items applied or confirmed pre-existing. Fix? column updated to DONE/SKIP.
No .cpp, .cl, or CMakeLists.txt files were modified.
```

### Changed Files

See `workflow/tasks/ux_audit_report.md` § Changed Files for full list (30 files).

### Remaining

None — task complete.
